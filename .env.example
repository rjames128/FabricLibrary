# Fabric Library Environment Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file with real secrets to source control

# ============================================
# REQUIRED: Authentication & Security
# ============================================

# JWT Secret - Secret key for signing JWT tokens
# Generate a strong random value with: openssl rand -base64 32
# KEEP THIS SECRET! Never commit the actual value to source control.
JWT_SECRET=your-very-long-random-secret-key-change-this-in-production

# Google OAuth Client ID - For validating Google ID tokens
# Obtain from: https://console.cloud.google.com/apis/credentials
# See backend/README.md for setup instructions
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# ============================================
# OPTIONAL: Additional Configuration
# ============================================

# SendGrid (Email) - For future milestones
# SENDGRID_API_KEY=SG.your-sendgrid-api-key

# Cloud Storage (Images) - For future milestones
# AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=...
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key

# ============================================
# NOTES
# ============================================
# 
# Database Connection:
#   When using .NET Aspire orchestration (recommended), the database connection
#   is automatically configured. No manual setup required.
#
# User Secrets (Alternative):
#   For local development, you can use .NET User Secrets instead of .env:
#   cd backend/FabricLibrary.Backend
#   dotnet user-secrets set "JWT_SECRET" "your-secret-here"
#   dotnet user-secrets set "GOOGLE_CLIENT_ID" "your-client-id-here"
#
# Production:
#   Use proper secret management (Azure Key Vault, AWS Secrets Manager, etc.)
#   instead of .env files in production environments.
